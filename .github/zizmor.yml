# SPDX-FileCopyrightText: The discro authors
# SPDX-License-Identifier: CC0-1.0

rules:
  unpinned-uses:
    # TODO: Pin SHA of all untrusted GitHub actions.
    ignore:
      - audit-dependencies.yaml
      - pre-commit.yaml
      - test.yaml

  superfluous-actions:
    # TODO: Replace obsolete actions.
    ignore:
      - pre-commit.yaml:37:15
      - test.yaml:32:15
